### 用户登录

url  :  http://localhost:8081/user/login

type: post

data：{ "username":xxx, "password":xxx }

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "uid": 6,
        "username": "张三",
        "password": "",
        "status": true
    }
}
```

```js
{
    "status": 0,
    "msg": "用户名或密码为null",     // msg 里面存放用户登录失败的原因
    "data": ""
}
```

error：

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

### 用户注册

url ：http://localhost:8081/user/register

type：post

data : { "username" : xxx , "password1" : xxx ，"password2" : xxx}

success：

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "result": "success",
        "redirect": "/login.html"
    }
}
```

```js
{
    "status": 0,
    "msg": "该账号已被注册！",
    "data": ""
}
```

error：

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

### 用户注销

url : http://localhost:8081/user/cancel

type : get

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "result": "success",
        "message": "注销成功!"
    }
}
```

error :

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

### 获取用户信息

url : http://localhost:8081/user/getuser

type : get

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "uid": 6,
        "username": "张三",
        "password": "",
        "status": true
    }
}
```

error :

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

### 音乐上传

url：http://localhost:8081/music/upload

type ：post

data : { "singer": xxx , "filename":xxx }          **这里的  filename  是一个  MultipartFile  文件类型**

success：

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "result": "success",
        "redirect": "list.html"
    }
}
```

```js
{
    "status": 0,
    "msg": "歌曲已在音乐库中!",
    "data": ""
}
```

error:

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

### 删除单个音乐

url : http://localhost:8081/music/delete

type : post

data ：{ "mid"：xxx }

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "result": "success",
        "musicTotal": 2,          //剩余音乐的数量
        "message": "删除成功"
    }
}
```

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "result": "fail",
        "message": "不是管理员，无法删除!"
    }
}
```

```js
{
    "status": 0,
    "msg": "删除失败,数据库中没有音乐！",
    "data": ""
}
```

error : 

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

### 批量删除音乐

url : http://localhost:8081/music/deletesel

type : post

data : { "id[]":xxx ，"id[]":xxx, ....}            **这里前端传过来的是一个数组，里面存放了需要删除的 id 号**

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "result": "success",
        "delete_total": 3,
        "message": "批量删除成功！"      // 全部删除成功
    }
}
```

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "result": "error",
        "delete_total": 2,            // 删除条数
        "message": "成功删除部分音乐"    // 成功删除部分
    }
}
```

```js
// 一条都没删除
{
    "status": 0,
    "msg": "批量删除失败!",
    "data": ""
}
```

error : 

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

### 查询音乐

pageNum=1 : 表示查询第一页 （查询第一页）

pageSize=8 : 表示当前页能存放的音乐数据条数（每一页8条数据）

**模糊查询：**

url：http://localhost:8081/music/getmusic?pageNum=1&pageSize=8&musicname=处

type : get

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "num": 1,
        "musicList": [
            {
                "mid": 43,
                "mname": "处处吻",
                "msinger": "迪丽热巴",
                "createtime": "2022-10-21",
                "url": "http://localhost:8080/music/处处吻.mp3",
                "uid": 6,
                "userInfo": {
                    "uid": 6,
                    "username": "张三",
                    "password": null,
                    "status": true
                }
            }
        ]
    }
}
```

**查询全部:**

url：http://localhost:8081/music/getmusic?pageNum=1&pageSize=8

type : get

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "num": 4,
        "musicList": [
            {
                "mid": 44,
                "mname": "Superstar",
                "msinger": "迪丽热巴",
                "createtime": "2022-10-21",
                "url": "http://localhost:8080/music/Superstar.mp3",
                "uid": 6,
                "userInfo": {
                    "uid": 6,
                    "username": "张三",
                    "password": null,
                    "status": true
                }
            },
            {
                "mid": 43,
                "mname": "处处吻",
                "msinger": "迪丽热巴",
                "createtime": "2022-10-21",
                "url": "http://localhost:8080/music/处处吻.mp3",
                "uid": 6,
                "userInfo": {
                    "uid": 6,
                    "username": "张三",
                    "password": null,
                    "status": true
                }
            },
            {
                "mid": 42,
                "mname": "相信",
                "msinger": "迪丽热巴",
                "createtime": "2022-10-21",
                "url": "http://localhost:8080/music/相信.mp3",
                "uid": 6,
                "userInfo": {
                    "uid": 6,
                    "username": "张三",
                    "password": null,
                    "status": true
                }
            },
            {
                "mid": 41,
                "mname": "出山",
                "msinger": "迪丽热巴",
                "createtime": "2022-10-21",
                "url": "http://localhost:8080/music/出山.mp3",
                "uid": 6,
                "userInfo": {
                    "uid": 6,
                    "username": "张三",
                    "password": null,
                    "status": true
                }
            }
        ]
    }
}
```

```js
{
    "status": 0,
    "msg": "查询结果为空!",
    "data": ""
}
```

error :

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```



### 收藏音乐

url ：http://localhost:8081/lovemusic/collectmusic

type ：post

data : { "id":xxx }

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "result": "success",
        "message": "收藏音乐成功"
    }
}
```

```js
{
    "status": 0,
    "msg": "重复收藏!",
    "data": ""
}
```

error : 

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

### 查询收藏音乐

pageNum=1 : 表示查询第一页 （查询第一页）

pageSize=8 : 表示当前页能存放的音乐数据条数（每一页8条数据）

**模糊查询：**

url ：http://localhost:8081/lovemusic/getlovemusic?pageNum=1&pageSize=8&musicname=处

type : get

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "num": 1,
        "musicList": [
            {
                "mid": 47,
                "mname": "处处吻",
                "msinger": "迪丽热巴",
                "createtime": "2022-10-21",
                "url": "http://localhost:8080/music/处处吻.mp3",
                "uid": 6,
                "userInfo": {
                    "uid": 6,
                    "username": "张三",
                    "password": null,
                    "status": true
                }
            }
        ]
    }
}
```

```js
{
    "status": 0,
    "msg": "用户收藏音乐为空",
    "data": ""
}
```

**查询全部：**

url ：http://localhost:8081/lovemusic/getlovemusic?pageNum=1&pageSize=8

type : get

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "num": 2,
        "musicList": [
            {
                "mid": 47,
                "mname": "处处吻",
                "msinger": "迪丽热巴",
                "createtime": "2022-10-21",
                "url": "http://localhost:8080/music/处处吻.mp3",
                "uid": 6,
                "userInfo": {
                    "uid": 6,
                    "username": "张三",
                    "password": null,
                    "status": true
                }
            },
            {
                "mid": 46,
                "mname": "相信",
                "msinger": "迪丽热巴",
                "createtime": "2022-10-21",
                "url": "http://localhost:8080/music/相信.mp3",
                "uid": 6,
                "userInfo": {
                    "uid": 6,
                    "username": "张三",
                    "password": null,
                    "status": true
                }
            }
        ]
    }
}
```

```js
{
    "status": 0,
    "msg": "用户收藏音乐为空",
    "data": ""
}
```

error : 

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

### 移除收藏的音乐

url : http://localhost:8081/lovemusic/removelovemusic

type : post

data : { "id":xxx }

success : 

```js
{
    "status": 1,
    "msg": "",
    "data": {
        "result": "success",
        "musicTotal": 1,
        "message": "取消收藏成功!"
    }
}
```

```js
{
    "status": 0,
    "msg": "音乐取消收藏失败",
    "data": ""
}
```

error : 

```js
{
    "status": 0,
    "msg": "异常信息:xxxx",     // msg 里面存放异常信息
    "data": ""
}
```

****

### 统一返回值处理

```java
// 统一返回类型处理
@ControllerAdvice
public class ReturnTypeInterceptor implements ResponseBodyAdvice {

    @Override
    public boolean supports(MethodParameter returnType, Class converterType) {
        // 如果触发了异常拦截，就不用进行统一处理
        String name = returnType.getMethod().getName();
        if("exceptionInterceptor".equals(name)){
            return false;
        }
        // 如果是播放音乐，就不经过统一类型返回
        if("getMusic".equals(name)){
            return false;
        }
        return true;
    }



    @Override
    public Object beforeBodyWrite(Object body, MethodParameter returnType,
                                                   MediaType selectedContentType,
                                                   Class selectedConverterType,
                                                   ServerHttpRequest request,
                                                   ServerHttpResponse response) {

        Object obj = null;
        // 通过判断是否是一个 String 类型来决定是否有错误信息
        if(body instanceof String){
            obj = Result.fail(body);
        } else{
            obj = Result.success(body);
        }
        return obj;
    }
}
```

### 统一异常处理

```java
// 对异常进行统一处理
@RestControllerAdvice
public class ExceptionInterceptor {

    @ExceptionHandler(Exception.class)
    public Object exceptionInterceptor(Exception e){
        HashMap<String,Object> map = new HashMap<>();
        map.put("status",0);
        map.put("data","");
        map.put("msg","异常信息:"+e.getMessage());
        return map;
    }
}
```

### 用户登录验证

```java
// 用户身份验证拦截器
@Component
public class UserLoginIdInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request,
                             HttpServletResponse response,
                             Object handler) throws Exception {

        // 1. 获取 session
        HttpSession session = request.getSession(false);
        // 2. 判断
        if(session == null || session.getAttribute(Constant.USER_INFO_SESSION_KEY) == null){
            // 没有登录就重定向到 用户登录页面
            response.sendRedirect("/login.html");
            return false;
        }
        return true;
    }
}
```

### 检查上传的音乐文件是否是 mp3 格式

```java
/**
 * 用来检查文件是否是 mp3 类型
 */
public class CheckTypeMP3 {

    /**
     * 是 MP3 就返回 true, 不是就返回 false
     */
    public boolean checkTypeMP3(File path){

        byte[] bytes = new byte[128];

        try(
                RandomAccessFile randomAccessFile = new RandomAccessFile(path,"r");
                ){

            // 长度不够不可能是 正常的mp3 文件
            if(randomAccessFile.length() <= 128){
                return false;
            }
            // 将指针指向 ID3V1 中 128 个字节的 第一个字节处
            randomAccessFile.seek(randomAccessFile.length()-128);
            // 读取
            randomAccessFile.read(bytes,0,3);

            if("TAG".equalsIgnoreCase(new String(bytes,0,3))){
                return true;
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
        return false;
    }
}
```

### 对数据库里的用户密码进行加密（先使用 BCrypt（md5+随机盐） 加密，然后将生成的密文 使用 非对称密钥 进行加密）

**生成 公钥 和 私钥 ( 使用 hutool 工具 )**

```java
public class Write {
    public void write(){
        // 通过 hutool 中的 SecureUtil 生成 公钥 和 私钥
        KeyPair pair = SecureUtil.generateKeyPair("RSA");
        PrivateKey aPrivate = pair.getPrivate();
        PublicKey aPublic = pair.getPublic();
       
        // 将 公钥 和 私钥 转换为 String ，并写入到文件中
        writeDetails(Base64.encode(aPrivate.getEncoded()),Base64.encode(aPublic.getEncoded()));
	}

    // 使用 IO流 来写文件
    public void writeDetails(String pri,String pub){
        System.out.println(pri);
        System.out.println(pub);
        try(
                Writer writerPri = new FileWriter("f:/private.txt");
                Writer writerPub = new FileWriter("f:/public.txt");
                ){

            writerPri.write(pri);
            writerPub.write(pub);

        } catch (IOException e) {
            e.printStackTrace();
        }
    }

    public static void main(String[] args) {
        Write write = new Write();
        write.write();
    }
}
```

**读取 公钥 和 私钥**

```java
   // 字符流读取 密钥
    public String[] getKey(){
        String[] strings = new String[2];
        try(
                BufferedReader readerPri = new BufferedReader(new FileReader(privateKeyUrl));
                BufferedReader readerPub = new BufferedReader(new FileReader(publicKeyUrl))
           ){

            String readLine = null;
            StringBuilder sb = new StringBuilder();
            // 每次读取一行
            while ((readLine = readerPri.readLine()) != null) {
                sb.append(readLine);
            }
            // 将读取的第一个 钥匙 放到 数组 中
            strings[0] = sb.toString();

            // 将临时变量 sb 清空,用来存储 另一把钥匙
            sb.delete(0,sb.length());
            // 每次读取一行
            while ((readLine = readerPub.readLine()) != null) {
                sb.append(readLine);
            }
            // 将读取的第一个 钥匙 放到 数组 中
            strings[1] = sb.toString();

        } catch (IOException e) {
            e.printStackTrace();
        }
        return strings;
    }
```

**加密 解密**

```java
    // 加密
    public String encrypt(String password){
        // 获取 公钥和私钥
        String[] key = getKey();

        rsa = new RSA(key[0],key[1]);
        // 通过 BCrypt (md5 + 随机加盐) 进行加密
        String encode = bCryptPasswordEncoder.encode(password);
        // 通过 非对称加密 再次包裹一层
        byte[] decrypt = rsa.encrypt(encode, KeyType.PublicKey);
        // 返回 密文
        return Base64.encode(decrypt);
    }

    // 解密
    public boolean decrypt(String nowPassword,String oldPassword){
        // 获取 公钥和私钥
        String[] key = getKey();
        rsa = new RSA(key[0],key[1]);
        // 通过非对称加密进行解密
        String result = rsa.decryptStr(oldPassword, KeyType.PrivateKey);
        // 通过 matches 方法来判断 输入的密码是否 就是 BCrypt 加密之前的密码
        return bCryptPasswordEncoder.matches(nowPassword, result);
    }
```